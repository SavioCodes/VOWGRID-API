name: Deploy Production

on:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set image tags
        run: |
          OWNER_LOWER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          {
            echo "OWNER_LOWER=${OWNER_LOWER}"
            echo "API_IMAGE=ghcr.io/${OWNER_LOWER}/vowgrid-api-api:production-${{ github.sha }}"
            echo "WEB_IMAGE=ghcr.io/${OWNER_LOWER}/vowgrid-api-web:production-${{ github.sha }}"
          } >> "$GITHUB_ENV"

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push API image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./apps/api/Dockerfile
          push: true
          tags: |
            ${{ env.API_IMAGE }}
            ghcr.io/${{ env.OWNER_LOWER }}/vowgrid-api-api:production

      - name: Build and push Web image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./apps/web/Dockerfile
          push: true
          tags: |
            ${{ env.WEB_IMAGE }}
            ghcr.io/${{ env.OWNER_LOWER }}/vowgrid-api-web:production

      - name: Configure SSH agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.PRODUCTION_DEPLOY_SSH_KEY }}

      - name: Copy release assets
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.PRODUCTION_DEPLOY_USER }}@${{ secrets.PRODUCTION_DEPLOY_HOST }} "mkdir -p '${{ secrets.PRODUCTION_DEPLOY_PATH }}/infra'"
          scp -o StrictHostKeyChecking=no -r infra/docker-compose.release.yml infra/Caddyfile infra/.env.production.example infra/api.env.example infra/web.env.example infra/observability ${{ secrets.PRODUCTION_DEPLOY_USER }}@${{ secrets.PRODUCTION_DEPLOY_HOST }}:${{ secrets.PRODUCTION_DEPLOY_PATH }}/infra/

      - name: Deploy production stack
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.PRODUCTION_DEPLOY_USER }}@${{ secrets.PRODUCTION_DEPLOY_HOST }} "
            set -eu && \
            cd '${{ secrets.PRODUCTION_DEPLOY_PATH }}' && \
            test -f infra/.env && \
            test -f infra/api.env && \
            test -f infra/web.env && \
            export VOWGRID_API_IMAGE='${{ env.API_IMAGE }}' && \
            export VOWGRID_WEB_IMAGE='${{ env.WEB_IMAGE }}' && \
            docker compose --env-file infra/.env -f infra/docker-compose.release.yml pull && \
            docker compose --env-file infra/.env -f infra/docker-compose.release.yml up -d postgres redis && \
            docker compose --env-file infra/.env -f infra/docker-compose.release.yml run --rm api pnpm migrate && \
            docker compose --env-file infra/.env -f infra/docker-compose.release.yml up -d --remove-orphans && \
            docker compose --env-file infra/.env -f infra/docker-compose.release.yml exec -T api wget -qO- http://127.0.0.1:4000/v1/health >/dev/null && \
            docker compose --env-file infra/.env -f infra/docker-compose.release.yml exec -T web wget -qO- http://127.0.0.1:3000 >/dev/null
          "
